# Dossier v2 (corrected after audit): collision.p0.symbol.type.type-appraisalpolicy

## Correction trail (kept visible on purpose)
* v1 recommended HOMONYM_RENAME/QUALIFY from operation-name inference alone, before reading
  the actual definition records. Audit found three records v1 never consulted:
  1. `symbol-adjudication-packets.jsonl`: both occurrences share ONE definition_digest
     (1bad2f35…) — the projections are literally identical.
  2. `policy-contract-classification-candidates.jsonl`: upstream lane classifies both as
     APPRAISAL_DECISION_POLICY / CLAIM_APPRAISAL_RULE_SET with shared non-collapse laws
     (declaration-not-evaluation, applicability-not-satisfaction, decision-not-enforcement…).
  3. `occurrence-disposition-candidates.jsonl`: per-side relation enum includes
     IMPORT_EXACT / IMPORT_WITH_PROFILE / QUALIFIED_LOCAL_HOMONYM; the experimentation side
     already carries a local-profile candidate string about authority scope, edition and
     effect adjudication — i.e., the corpus anticipates a profiled import there, not a rename.

## Corrected analysis
Identical digest plus upstream classification outweigh operation-name vibes. The right
primary disposition is sharing, not splitting:

* Canonical owner candidate: **context.lpe.independent-appraisal → library.lpe.rats-appraisal**
  holds `AppraisalPolicy` with RFC 9334-backed appraisal semantics (verified register source
  src.ietf.rfc9334); its contract record names `AppraisalPolicyEdition`.
* Experimentation side relation: **IMPORT_WITH_PROFILE**
  (`library.experiment.conclusion.appraiser` keeps authority-scope/edition/effect profile per
  its own local_profile_candidates; no alias, import by exact digest).
* Standing fallback (explicitly kept open): if owner publication reveals the strength-lattice
  grammar cannot live in one carrier, downgrade to QUALIFIED_LOCAL_HOMONYM with
  ClaimStrengthLatticePolicy — this becomes a decision dependency, not today's answer.
* Third symbol `SelectionAppraisalPolicy`: unrelated, untouched.

## Falsification attempts against v2
1. "Import weakens experimentation autonomy": rejected — profile mechanism exists precisely
   to carry local authority scope without duplicating meaning.
2. "RFC 9334 is attestation-specific so cannot host claim-appraisal rules": rejected — the
   classified semantic position is the abstract CLAIM_APPRAISAL_RULE_SET; RFC anchors policy
   mechanics, lattice specifics stay profile-local.
3. v1 rename proposal: self-falsified (would create two divergent copies of an identical
   projection and violate the corpus import-required classification).

## Receipt required to actually close
SEMANTIC_OWNER selection between {shared-owner(import-with-profile), qualified-homonym split}
recorded against docket.p2.symbol.type-appraisalpolicy.v1; then registry regeneration.

Status after correction: DECISION_READY_UNRATIFIED (corrected recommendation above).
