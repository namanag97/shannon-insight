# GPT Pro product-ontology delta intake audit

The files in `output-2026-08-27/` are preserved verbatim as an external research candidate. They
are not canonical registry mutations and do not qualify implementations, providers, products or
vertical acceptances.

## Intake verdict

`ACCEPT_CANDIDATE_OVERLAY_REBASE_REQUIRED`

- The supplied directory contains the expected 18 JSONL artifacts plus its reports and prose.
- Its internal report accounts for 686 quotients, 16,687 atoms, 625 research-addressable
  quotients, 674 exact-contract candidates and 61 downstream evidence gates.
- Independent intake parsing confirms the advertised JSONL record counts, unique primary IDs,
  `completion_claim:false`, and zero invented qualification/completion counts.
- The original ZIP whose claimed SHA-256 is
  `87c09e040c8b90d27003c0f442f893f4747e33ea1f8889aa0d9ce14a9078fd4e` was not present in the
  supplied Downloads path, so that archive digest cannot be verified. The extracted directory is
  the preserved input.
- The package's own `input-inventory.json` identifies a different source archive with SHA-256
  `cb7d9f955035af1637a67c88553a1a655a8a1453ffc846d5d9ee06efc90bb210`; the current repository
  handoff ZIP has SHA-256 `5da7f7e0ff1c3f5ab7b8d814aa1caa5ffc187b80e357afc0816268bcef74c58a`.
- Against the current worktree, 2,196 of 2,356 inventoried input files match exactly and 160 have
  changed. No inventoried path is missing.
- The delta was produced over a 61-product projection. The current canonical candidate corpus has
  64 retained products and already promotes Dataset Curation, Integrated Planning and Image
  Analysis, so their `PBD-NEW-*` rows are historical proposals rather than current additions.

## Integration law

Do not overwrite canonical files from this directory. Rebase each decision by stable subject,
library, family, symbol and source identifiers; compare it to the current owner/boundary decision;
retain local residuals; route conflicts to adjudication; then regenerate canonical projections
from their Python source models. A matching old input digest permits evidence reuse but never
automatic ratification.

Run `python3 audit_delta.py` from this directory to repeat the structural and snapshot-drift audit.

