# Independent appraisal and continued global product-ontology convergence

## Execution directive

You are receiving a machine-readable research corpus for a proposed composable enterprise data and
analytics company. Inspect the complete attached ZIP before forming conclusions. Execute the
research assignment below; do not merely summarize the corpus, praise its ambition, restate the
prompt, produce a generic architecture, or propose another research plan.

Use current internet research extensively. Prefer primary sources: standards, normative
specifications, original papers, official project documentation, conformance suites, regulatory or
industry authorities, and technically specific product documentation. Sources support bounded
claims; they do not become the semantic owner of this corpus.

This package includes the output of a prior research pass. That pass reported dispositions for all
625 research-addressable quotients. The repository has deterministically rebased those records as
`PROPOSED_UNRATIFIED`; it has not accepted them as truth. Your first responsibility is to appraise,
falsify, correct, or explicitly uphold those proposed decisions against the current corpus. Do not
repeat the old pass merely to reproduce its counts, and do not treat agreement between the input
and prior output as independent evidence.

Return one downloadable ZIP containing the required files. Do not implement a compiler, write a
platform application, generate Rust crates, claim provider qualification, or modify the input
corpus. Your job is to discover, challenge, decompose and encode globally useful research deltas
that a later adjudication process can merge.

## Goal

Determine what is still missing, incorrectly combined, incorrectly split, weakly evidenced or
wrongly wired in the attached product, capability, library and semantic corpus so that it can
eventually describe any enterprise data-and-analytics solution through declarations and exact,
composable contracts.

The desired system will eventually allow an enterprise intent to select and compose:

```text
industry and organization-specific intent
        -> application / analytical use case
        -> product boundaries and operated lifecycles
        -> capabilities and published contracts
        -> semantic/effect/test libraries
        -> qualified provider implementations
        -> deployment and runtime plans
        -> evidence-backed acceptance or typed refusal
```

This assignment concerns the research corpus and correct boundaries. It does **not** concern
building that compiler or pretending that researched specifications are executable implementations.

## Starting state—challenge it, do not inherit it

The attached corpus currently reports:

- 77 global boundary candidates and 64 retained product boundaries;
- 64 complete candidate product DDD dossiers;
- 518 exact product-library subjects;
- 674 exact-contract dockets;
- 505 qualification scopes and 1,010 independent implementation slots;
- 888 open qualification-evidence vacancies;
- 184 blocking product-closure work items;
- 1,024 product/vertical-slot/acceptance-gate obligations;
- 686 gap-topology quotients representing 16,732 open atoms;
- 625 research-addressable quotients/14,496 atoms rebased as proposed and unratified;
- 61 physical/governance quotients/2,236 atoms still evidence-gated;
- 19 encoded analytical/domain-formalism semantic slices;
- 1,037 unique named expert research routes and 83 specialist company/project records in
  non-ranking, open-world navigation indexes;
- zero qualified providers, zero portable offers, zero executed vertical acceptances, zero
  build-ready products and zero ratified products.

Recent changes separated:

- graph/network analysis methods from a Graph Analysis Workbench product;
- Master Data Governance from Reference Data Governance;
- Data Quality Operations from Reconciliation and Control Operations;
- Integrated Planning Workbench from Forecasting Workbench, Optimization, Simulation, generic
  Workflow, authority issuance and operational execution.

These are candidate conclusions, not facts you must preserve. Independently challenge them. Never
restore an obsolete combined identity merely to resolve a reference, and never accept a split just
because it already appears in the corpus.

Dataset Curation and Image Analysis Workbench are now retained candidates. Image Analysis owns 14
exact pure contract seams. Challenge these additions and discover remaining missing product and
non-product boundaries systematically.

## Required reasoning frame

Do not process hundreds of records one by one. Derive orthogonal semantic axes and use them to form
reversible research quotients. One researched decision kernel may cover many exact occurrences only
when they genuinely share the same question, owner, bearer, formalism, authority requirement,
closure operation, refusal behavior and conformance oracle. Falsify every proposed group and split
it on the first material counterexample.

For every proposed product boundary test at least:

1. sovereign user question and measurable job/outcome;
2. independent adoption, operation, support, economics and exit;
3. owned state, lifecycle and time model;
4. semantic authority and operational/effect authority;
5. harmed parties, human judgment and appeal/reversal;
6. exact inputs, outputs, ports and published language;
7. independently replaceable provider implementations;
8. invariants, refusals, negative twins and non-collapse laws;
9. cross-context dependencies and anti-corruption layers;
10. evidence that would falsify the proposed boundary.

Use DDD where it is truthful for language, ownership, aggregates, lifecycle, invariants and context
maps. Also use the better local formalism when appropriate: type theory, algebra, state machines,
temporal and bitemporal models, process/case models, evidence and argumentation theory, measurement,
statistics, causal inference, operations research, optimization, simulation, control theory,
information retrieval, graph theory, spatial models, signal processing, protocol specifications,
security/privacy/safety engineering, Wardley/estate mapping, service blueprints, Team Topologies,
event storming and value-stream analysis. DDD is not the master method.

Keep these identities distinct:

```text
domain phenomenon != representation != observation != assertion != evidence
method/formalism != semantic library != capability != operated product
semantic owner != evidence authority != operational authority != implementation provider
proposal != decision != authorization != effect != receipt
source record != entity-resolution assertion != master identity != master projection
reference concept != code != designation != value set != directional mapping
metric/formula != query plan != visualization != report/dashboard product
quality signal != defect != reconciliation break != correction authority
graph method != ontology governance != graph-analysis workspace
prediction != causal claim != optimization proposal != governed decision
```

Do not prefix everything with AI. Deterministic, statistical, heuristic, optimization, simulation
and predictive methods remain first-class. Models, LLMs and agents may be optional or explicitly
required capabilities at particular use sites, but never acquire semantic ownership, authority,
effect execution or acceptance-proof responsibility. Removing every optional model/agent extension
must leave a complete deterministic core wherever the domain permits one.

## Coverage audit

Use the attached corpus to perform a global missingness and boundary audit across, at minimum:

- every enterprise data-source and source-system family;
- structured, semi-structured, unstructured, graph, spatial, temporal, event, signal, image,
  document, communication, scientific, operational and derived data shapes;
- connectivity, capture/CDC, ingestion, transformation, orchestration, dataflow, validation,
  storage, table/catalog, transactions, query, search, serving and activation;
- metadata, schema, contracts, glossary, ontology, master data, reference data, identity resolution,
  lineage, quality, reconciliation, policy, privacy, security, safety, publication and exchange;
- descriptive, diagnostic, investigative, process/object-centric, statistical, causal, predictive,
  forecasting, optimization, simulation, queueing, graph/network, geospatial, signal, image,
  document/text, retrieval, semantic-metric, BI, visualization and decision analytics;
- human workflows, cases, review, adjudication, planning, annotation, curation, inspection,
  assurance and operational handoff;
- runtime compute, persistence, messaging, scheduling, quotas, resources, failures, observability,
  recovery, cost and portability;
- every major industry and subindustry as a falsification and applicability test.

Industry research must uncover real analytical cases—not only KPIs or generic dashboards. Look for
industry-specific diagnostic, investigation, reconciliation, risk, process, optimization,
simulation, inspection, planning, forecasting, control, scientific and decision problems; their
source systems, data shapes, authorities, time/grain semantics, methods, outputs and actions.
Industry-specific vocabulary, rules and authority should normally live in solution packs unless
the evidence proves an independently operated horizontal product boundary.

Do not pursue arbitrary source-count quotas. Research until the bounded claim, authority limit,
negative twin, counterexample search and invalidation condition are adequately supported. Record
thin or contradictory evidence as a vacancy instead of filling it with prose.

## Required output ZIP

Create a directory named `GPT-PRO-PRODUCT-ONTOLOGY-DELTA/` and return it as one ZIP. All JSONL must
be UTF-8, one independently parseable object per line, deterministically ordered by its primary ID.
All records must contain `status`, `source_refs`, `counterexample_or_negative_twin`,
`invalidation_condition` and `completion_claim:false` where applicable.

### 1. `EXECUTIVE-VERDICT.md`

State what the corpus gets right, the most consequential errors, the largest missing universes,
which current product boundaries should be retained/split/merged/narrowed/reclassified, and the
smallest next research waves. Distinguish structural completeness, semantic evidence, exact
contracts, implementations, provider qualification and vertical acceptance.

### 2. `input-inventory.json`

List every consumed input path, its SHA-256 digest, record count or byte count, role, and whether it
materially informed the result. Explicitly list exclusions and reasons.

### 3. `source-register.jsonl`

One row per source with: `source_id`, title, organization/authors, publication/edition date,
retrieval date, URL/DOI, source class, exact bounded claim supported, authority limit, affected
records and contradictory evidence. Do not cite search-result pages.

### 4. `product-boundary-deltas.jsonl`

One row for every retained, new, split, merge, narrow, reclassify or retire recommendation with:

`delta_id`, `subject_refs`, `operation`, `proposed_product_refs`, `sovereign_questions`,
`independent_adoption_test`, `owned_lifecycles`, `owned_state`, `semantic_authorities`,
`effect_authorities`, `inside`, `outside`, `neighbor_relationships`, `responsibility_migration`,
`exit_obligations`, `evidence_refs`, `counterexamples`, `falsifiers`, `confidence`, `status`.

No compatibility aliases. Every prior responsibility must migrate to exactly one proposed owner or
remain an explicit conflict/vacancy.

### 5. `product-dossier-deltas.jsonl`

For every new or materially changed product, encode the full product truth and DDD dossier used by
the input corpus, including the strategic/tactical fields, automation modality, human/process,
data/intelligence, authority/trust, security/safety, production, ecosystem, change/delivery,
implementation/build and migration/proof obligations. Do not duplicate unchanged dossiers.

### 6. `library-boundary-deltas.jsonl`

For every missing, overloaded, misplaced or redundant composable library boundary encode:

`library_delta_id`, `operation`, `library_refs`, `proposed_library_ref`, `semantic_question`,
`owner_context`, `carrier_types`, `operations`, `decisions_exposed`, `invariants`, `refusals`,
`effect_boundary`, `time_model`, `dependencies`, `public_ports`, `conformance_oracles`,
`product_imports`, `vertical_extension_points`, `evidence_refs`, `status`.

Libraries are pure composable contracts, not applications. Provider selection, configuration,
policy and domain variation must be explicit decision inputs rather than name branches or hidden
defaults.

### 7. `semantic-decision-kernels.jsonl`

Encode the smallest reusable, falsifiable semantic decisions discovered across the corpus. Include
the decision signature, owner candidate, formalism, bearer/decision locus, authority requirement,
law/refusal/oracle, evidence and negative twins.

### 8. `lossless-quotient-projection.jsonl`

Map every affected exact input occurrence to one primary semantic-decision kernel plus every local
residual. This projection must be reversible; do not merge by names, prose similarity, shared
sources or package adjacency.

### 9. `capability-product-library-wiring-deltas.jsonl`

Encode exact proposed edges among products, capabilities, libraries, semantic owners, provider
requirements, solution packs and vertical compositions. State binding phase, cardinality,
authority effect, compatibility/evolution requirement and typed refusal. Never invent a qualified
offer or implementation.

### 10. `vertical-falsification-cases.jsonl`

Provide high-value industry/subindustry cases selected to falsify horizontal boundaries. Each row
must include industry/subindustry, real problem—not merely a KPI—actors, decision/action, source
systems, data shapes, grain, time model, methods/formalisms, required products/libraries, vertical
vocabulary/rules/authority, failure modes, acceptance evidence and which boundary hypothesis it
tests. Favor diversity and boundary power over superficial enumeration.

### 11. `conflicts-vacancies-and-open-questions.jsonl`

Every unresolved semantic owner, missing product/library, evidence conflict, missing source family,
missing data shape, missing method, missing route, unresolved exact contract, or untestable
assumption must be typed, prioritized by fanout/blast radius, and assigned an evidence need and
closure condition.

### 12. `convergence-dag.jsonl` and `CONVERGENCE-DAG.md`

Factor remaining research into the smallest dependency-ordered root work packages. Do not create
one work item per gap. Include root decision, dependent occurrences, fanout, prerequisites,
falsification test, expected artifacts, blocking state and next unblocked wave.

### 13. `validation-report.json`

Prove JSONL validity, unique IDs, referential integrity, source resolution, exact responsibility
migration, quotient reversibility, local-residual preservation, no silent aliases, no invented
qualification, and deterministic ordering. Include exact counts. Keep `completion_claim:false`.

## Final discipline

The output is a research delta, not canonical truth. Use statuses such as `RESEARCHED_CANDIDATE`,
`PROPOSED_UNRATIFIED`, `CONFLICT`, `EVIDENCE_VACANCY` and `OUTSIDE_SCOPE`. Do not mark anything
implemented, qualified, accepted, build-ready or ratified.

In your final response, provide the downloadable ZIP and a short factual summary of the highest-
impact corrections, new product/library candidates, quotient/root-work counts and unresolved
blockers. Do not return only prose when machine-readable artifacts were requested.
